input {
	tcp {
		port => 5002
		type => "json"
	}
}
filter {
	json {
        source => "message"
	}
}
output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "changeme"
		index => "demo-json-from-logstash"
		ecs_compatibility => disabled
	}
}
